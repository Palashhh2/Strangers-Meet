// Prisma configuration reference
// Prisma is installed but the schema.prisma file is currently empty.
// To use Prisma:
// 1. Add your database connection string to .env.local as DATABASE_URL
// 2. Define your data model in prisma/schema.prisma
// 3. Run: npx prisma migrate dev --name init
// 4. Run: npx prisma generate
//
// For now, the app uses JSON file storage at /data/
